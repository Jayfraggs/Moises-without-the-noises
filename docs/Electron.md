# Electron

Overview
- Thin Electron shell in `electron/main.js`. Launches the backend and loads the UI.

Constraints
- Electron must not contain business logic or audio processing; all logic lives in backend and frontend.